// MainApp.js
import React from "react";
import DarkModeApp from "./DarkModeApp"; // Import the DarkModeApp component

const MainApp = () => {
  return (
    <div>
      <DarkModeApp /> {/* Render the DarkModeApp component */}
    </div>
  );
};

export default MainApp;
